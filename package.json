{
  "name": "browserstack-issue",
  "version": "0.0.1",
  "private": true,
  "dependencies": {},
  "scripts": {
    "cypress:local": "cross-env NODE_TLS_REJECT_UNAUTHORIZED=0 cypress open --e2e --browser chrome",
    "cypress-browserstack": "browserstack-cypress run"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "browserstack-cypress-cli": "^1.20.0",
    "cross-env": "^7.0.3",
    "cypress": "^12.8.1",
    "typescript": "^4.8.4"
  }
}
